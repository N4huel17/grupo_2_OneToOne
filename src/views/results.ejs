<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('partials/head', {title: 'Resultados de tu búsqueda'}) %>
<body>
	<!-- Header -->
	<%- include('partials/headerNavbar') %>
	<!-- /Header -->
	
	<!-- Banner -->
	<div class="banner"></div>
	<!-- /Banner -->

	<!-- todos los productos -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Resultados de tu búsqueda: <%= keywords %></h2>
			</div>
			<% if (products.length) { %>
			 
				<% products.forEach(product => { %>
				 <%- include('./partials/cardProduct', {
					product
				 }) %>
				<% }) %>
				
			<% }else{ %>
			<div class="col-12">
				<h2 class="products-title" style="color: rgb(255, 28, 193);">Producto No Encontrado</h2>
			</div>
			<% } %>
		</div>
	</div>
	<!-- /todos los productos -->

	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->
</body>	
</html>